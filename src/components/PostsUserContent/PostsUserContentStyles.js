import { StyleSheet } from "react-native";
import { backgroundTextColor, primaryTextColor } from "../../assets/colors/colors";

export const styles = StyleSheet.create({
    postSectionContent: {
        paddingVertical: 32,
    },
    postThumb: {
        marginBottom: 32,
    },
})

