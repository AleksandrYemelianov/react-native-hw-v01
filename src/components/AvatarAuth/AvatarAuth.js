import { View, Text } from 'react-native'
import React from 'react'
import { styles } from './AvatarAuthStyle'

const AvatarAuth = () => {
  return (
    <View style={styles.wrapper}>
      <Text>AvatarAuth</Text>
    </View>
  )
}

export default AvatarAuth