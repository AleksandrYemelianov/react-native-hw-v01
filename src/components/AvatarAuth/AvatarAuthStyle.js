import { StyleSheet } from "react-native";
import { backgroundTextColor } from "../../assets/colors/colors";

export const styles = StyleSheet.create({
    wrapper: {
        position: 'absolute',
        // top: 0,
        // left: '50%',
        width: 120,
        height: 120,
        borderRadius: 16,
        // display: 'flex',
        // justifyContent: 'flex-end',
        // alignItems: 'center',
        backgroundColor: backgroundTextColor,
    },
});
